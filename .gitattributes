# The agent system prompt and its output schema are embedded byte-for-byte with
# include_str!, and both are compared against what Claude Code returns. A CRLF
# rewrite on checkout would silently change the prompt, so keep them as-is.
src-tauri/src/agent_instructions.md -text
src-tauri/src/response_schema.json -text
